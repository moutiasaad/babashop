/.phpunit.cache
/node_modules
/public/build
/public/hot
/public/storage
/storage/*.key
/vendor
.env
.env.backup
.env.production
.phpunit.result.cache
Homestead.json
Homestead.yaml
auth.json
npm-debug.log
yarn-error.log
/.fleet
/.idea
/.vscode
# Added by code-review-graph
.code-review-graph/

# Backups & DB dumps (never commit — use migrations for schema, OneDrive for backups)
/*.sql
/*.zip

# Runtime user content (never commit — belongs on the server)
/public/uploads/
/public/invoice/
